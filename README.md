# Sketch2Spec — Stage 1–4

เว็บแอปสำหรับอัปโหลดแปลนบ้าน ตรวจจับผนัง ประตู และหน้าต่างด้วยโมเดล `best.pt` ตรวจแก้ผลลัพธ์ในมุมมอง 2D/3D และเลือกวัสดุพร้อมประมาณงบเบื้องต้น

## ความต้องการของระบบ

- Node.js 20 ขึ้นไป
- Python 3.11
- Windows และ VS Code
- อินเทอร์เน็ตสำหรับติดตั้งแพ็กเกจครั้งแรก

โปรเจกต์ใช้พาธแบบ Relative จึงแตก ZIP ไปไว้ที่ไดรฟ์หรือชื่อโฟลเดอร์ใดก็ได้

## รัน Backend ครั้งแรก

เปิด Terminal ที่โฟลเดอร์หลักของโปรเจกต์:

```powershell
python -m venv .\backend\.venv
.\backend\.venv\Scripts\python.exe -m pip install --upgrade pip
.\backend\.venv\Scripts\python.exe -m pip install -r .\backend\requirements.txt
.\backend\.venv\Scripts\python.exe -m uvicorn main:app --reload --app-dir .\backend
```

ตรวจสอบ Backend:

```text
http://localhost:8000/health
http://localhost:8000/docs
```

ไฟล์โมเดลต้องอยู่ที่:

```text
backend/best.pt
```

## รัน Backend ครั้งถัดไป

```powershell
.\backend\.venv\Scripts\python.exe -m uvicorn main:app --reload --app-dir .\backend
```

## รัน Frontend ครั้งแรก

เปิด Terminal ใหม่ที่โฟลเดอร์หลัก:

```powershell
npm install
npm run dev
```

กรณี npm ในเครื่องขึ้น `Exit handler never called!` ให้ใช้ pnpm ผ่าน Corepack:

```powershell
corepack pnpm@latest-10 install
corepack pnpm@latest-10 run dev
```

เปิดเว็บ:

```text
http://localhost:3000/upload
```

## รัน Frontend ครั้งถัดไป

เลือกใช้คำสั่งเดียวกับตัวจัดการแพ็กเกจที่ติดตั้งสำเร็จครั้งแรก:

```powershell
npm run dev
```

หรือ:

```powershell
corepack pnpm@latest-10 run dev
```

## ตั้งค่า URL ของ Backend

ค่าเริ่มต้นคือ `http://localhost:8000` กรณีใช้พอร์ตอื่น ให้สร้าง `.env.local` ที่โฟลเดอร์หลัก:

```env
NEXT_PUBLIC_DETECTION_API_URL=http://localhost:8000
```

## ไฟล์ที่รองรับ

- JPG
- PNG
- WebP
- PDF หลายหน้า พร้อมเลือกหน้าที่ต้องการ

## Stage 1–3

- Upload, Crop, Rotate และเลือกหน้า PDF
- ตรวจจับผนัง ประตู และหน้าต่างด้วย `best.pt`
- แก้ไขผลตรวจจับใน 2D และ 3D
- สร้าง ย้าย ยืด หด และลบกำแพง
- สร้างห้อง ประตู และหน้าต่าง
- Snap แบบแม่เหล็ก กริด และอิสระ
- กด `Shift` ค้างขณะลากเพื่อปิด Snap ชั่วคราว
- กำแพงใหม่ Trim ที่ผิวกำแพงเดิมเพื่อลดการทับกัน
- ไม่มี Auto Merge กำแพงแต่ละชิ้นยังมี ID แยก
- Undo/Redo รองรับแป้นพิมพ์ไทยและอังกฤษ

## Stage 4 — วัสดุและงบประมาณ

เปิดมุมมอง 3D แล้วกดปุ่ม **วัสดุ & งบ**

รองรับวัสดุสำหรับ:

- ผนัง: สีขาวด้าน คอนกรีต อิฐ ไม้กรุ และหิน
- ประตู: สีขาว ไม้โอ๊ก และกรอบดำ
- หน้าต่าง: กระจกกรอบเงิน กรอบดำ และกรอบไม้
- พื้น: กระเบื้องสีอ่อน พื้นไม้ ปูนขัดมัน และกระเบื้องสีเข้ม

การใช้งาน:

- ใช้วัสดุกับวัตถุที่เลือก
- ใช้วัสดุกับวัตถุประเภทเดียวกันทั้งหมด
- เปลี่ยนวัสดุพื้นแบบทันที
- วัสดุของผนัง ประตู และหน้าต่างถูกบันทึกในโปรเจกต์
- วัสดุพื้นถูกบันทึกใน IndexedDB พร้อมโปรเจกต์
- Undo/Redo ใช้กับการเปลี่ยนวัสดุของวัตถุได้

## การประมาณราคา

ต้องกำหนดมาตราส่วนจริงก่อน ระบบจึงจะคำนวณราคาได้

- ผนังคำนวณจากความยาว × ความสูง × 2 ด้าน
- ประตูคำนวณเป็นจำนวนชิ้น
- หน้าต่างคำนวณตามพื้นที่โดยประมาณ
- พื้นคำนวณจากพื้นที่สี่เหลี่ยมที่ผู้ใช้สร้างเอง
- ส่งออกรายการวัสดุและราคาเป็นไฟล์ CSV ได้

ราคาเป็นค่าเริ่มต้นสำหรับการสาธิต สามารถแก้ไขได้ใน:

```text
lib/materials.ts
```

ผลลัพธ์เป็นการประมาณเบื้องต้น ไม่ใช่ BOQ สำหรับงานก่อสร้างจริง

## ปุ่มลัด Editor

- `Ctrl + Z` — Undo
- `Ctrl + Y` หรือ `Ctrl + Shift + Z` — Redo
- `Delete` — ลบวัตถุที่เลือก
- `Ctrl + D` — ทำสำเนาวัตถุ
- `1` — เครื่องมือเลือก
- `2` — เครื่องมือผนัง
- `R` — เครื่องมือห้อง
- `3` — เครื่องมือประตู
- `4` — เครื่องมือหน้าต่าง
- `5` หรือ `F` — เครื่องมือสร้างพื้น
- `G` — เปลี่ยนโหมด Snap
- `H` — เปิดหรือปิดคำแนะนำ
- `Esc` — กลับเครื่องมือเลือก

## ตรวจสอบโปรเจกต์

```powershell
npm run lint
npx tsc --noEmit
npm run build
```

หรือ:

```powershell
npm run check
```

ทดสอบ Backend:

```powershell
.\backend\.venv\Scripts\python.exe .\backend\test_api_smoke.py
```

## พอร์ตที่ใช้

```text
Frontend: http://localhost:3000
Backend:  http://localhost:8000
```

ต้องเปิด Backend และ Frontend แยกกันคนละ Terminal

## วัสดุสมจริง (Stage 4.1)

เวอร์ชันนี้รองรับทั้ง **procedural texture** และ **image-based PBR texture** ที่จัดเก็บอยู่ภายในโปรเจกต์
จึงยังสามารถพกโปรเจกต์ไปใช้เครื่องอื่นได้ โดยไม่ต้องเชื่อมต่อบริการ texture ภายนอก

สิ่งที่เพิ่มเข้ามา:

- ผนังมีลายผิวแบบ สีทา / คอนกรีต / อิฐ / ไม้ / หิน
- พื้นมีลาย กระเบื้อง / ไม้ / ปูนขัดมัน
- ประตูมีผิวไม้และผิวโลหะ
- หน้าต่างมีเอฟเฟกต์กระจกพร้อมกรอบ
- ปรับ repeat texture ตามขนาดวัตถุอัตโนมัติ เพื่อไม่ให้ลายยืด
- ใช้ `MeshPhysicalMaterial` เพื่อให้แสงและผิววัสดุดูสมจริงขึ้น
- มีภาพตัวอย่างลายวัสดุ (Thumbnail Preview) ในแผงวัสดุ ก่อนนำไปใช้กับโมเดล

หมายเหตุ:

- ระบบนี้เป็นแบบ **lightweight realistic** เหมาะกับการรันบนเว็บและเครื่องทั่วไป
- ถ้าต้องการระดับสมจริงมากกว่านี้ ภายหลังสามารถต่อยอดเป็น HDRI + PBR texture จริงได้

## สร้างพื้นเอง (Stage 4.2)

เวอร์ชันนี้ยกเลิกการสร้างพื้นห้องอัตโนมัติ และเปลี่ยนเป็นให้ผู้ใช้สร้างพื้นเอง

วิธีใช้:

1. เลือกเครื่องมือ **พื้น** หรือกด `5` / `F`
2. คลิกค้างแล้วลากบนแบบแปลนเพื่อสร้างพื้นสี่เหลี่ยม
3. กลับไปที่เครื่องมือ **เลือก** เพื่อย้ายพื้น
4. ลากจุดมุมสีม่วงเพื่อปรับความกว้างและความยาว
5. เปิด **วัสดุ & งบ > พื้น** เพื่อใช้วัสดุกับพื้นชิ้นที่เลือกหรือพื้นทั้งหมด

คุณสมบัติ:

- พื้นแต่ละชิ้นเป็นวัตถุแยกกัน
- ย้าย ปรับขนาด ทำสำเนา ลบ Undo และ Redo ได้
- ใช้วัสดุคนละแบบในแต่ละห้องได้
- พื้นใหม่ใช้วัสดุเริ่มต้นจากวัสดุพื้นล่าสุดที่ตั้งไว้
- ไม่มีพื้นขนาดใหญ่ปูเต็มแบบแปลนอัตโนมัติ


## Real Texture System (Stage 5.1)

เพิ่มวัสดุไม้จากภาพจริงที่ผู้ใช้ส่งมา พร้อมสร้างแผนที่ PBR สำหรับใช้งานบนโมเดล 3D:

- `walnut_basecolor.jpg` — สีและลายไม้จริง
- `walnut_normal.jpg` — รายละเอียดเสี้ยนและร่องไม้เมื่อรับแสง
- `walnut_roughness.jpg` — ควบคุมความด้านและความเงา
- `walnut_ao.jpg` — แผนที่เงาร่องสำหรับต่อยอด

วัสดุใหม่:

- ไม้กรุวอลนัตจริงสำหรับผนัง
- ประตูวอลนัตลายจริง
- พื้นไม้วอลนัตจริง

การใช้งาน:

1. เปิด **วัสดุ & งบ**
2. เลือกหมวดผนัง ประตู หรือพื้น
3. เลือกวัสดุที่มีป้าย `REAL`
4. กดใช้กับวัตถุที่เลือกหรือใช้กับทั้งหมด
5. กดไอคอนขยายบนภาพตัวอย่าง เพื่อดูพื้นผิวแบบซูมใกล้

ระบบจะปรับการทำซ้ำของลายตามขนาดวัตถุ และหมุนแนวลายสำหรับประตูโดยอัตโนมัติ เพื่อลดปัญหาลายไม้ยืดผิดสัดส่วน

## Stage 5 — Material Selection Complete

เวอร์ชันนี้ทำฟังก์ชันเลือกวัสดุให้ครบตามขอบเขต Stage 5:

- หมวดวัสดุ: กระเบื้องพื้น, สีทาผนัง, วอลเปเปอร์, งานไม้, ฝ้าเพดาน และกระจก/โลหะ
- Instant Apply: เปิดใช้งานเพื่อคลิกวัสดุแล้วอัปเดตโมเดล 3D และราคาในทันที
- Material Comparison: เลือกวัสดุได้สูงสุด 3 รายการเพื่อเปรียบเทียบราคา พื้นผิว และความด้าน
- Save Favorites: บันทึกวัสดุโปรดใน Browser และกรองดูเฉพาะรายการโปรดได้
- Texture Preview: เปิดดูพื้นผิวแบบซูมใกล้ พร้อมรองรับ Real Texture และ Procedural PBR
- Texture Controls: ปรับขนาดลายและหมุนทิศทางลายของวัตถุที่เลือกได้
- Lighting Presets: กลางวัน, อบอุ่น, กลางคืน และสตูดิโอ
- Ceiling Tool: ปุ่ม `6` หรือ `C` สำหรับสร้างฝ้าเพดานแบบสี่เหลี่ยม
- Ceiling Height: เลือกฝ้าแล้วเปิด **แก้ขนาด/ระดับ** เพื่อเลื่อนขึ้นลงด้วย Slider, กรอกเมตร, ปุ่ม ±10 ซม. หรือ Page Up/Page Down
- Real-time Calculation: เมื่อใช้วัสดุ ระบบคำนวณราคาใหม่จากวัตถุและพื้นที่ที่มีมาตราส่วนจริงทันที


## ตั้งมาตราส่วนแบบง่าย

- ไม่จำเป็นต้องเลือกกำแพงก่อน ระบบจะเลือกกำแพงที่ยาวที่สุดให้อัตโนมัติ
- กรอกความยาวจริงเพียงครั้งเดียว แล้วกด **ใช้กับทั้งแปลน**
- เลือกกำแพงอื่นเองได้ เมื่อต้องการใช้ระยะอ้างอิงที่แม่นยำกว่า
- มีปุ่มลัดความยาว 3, 4, 5 และ 6 เมตร
- กด Enter หลังกรอกตัวเลขเพื่อยืนยันได้
- ตั้งมาตราส่วนเพียงครั้งเดียวต่อหนึ่งแปลน

## การปรับระดับฝ้า

1. ใช้เครื่องมือ **ฝ้า** สร้างพื้นที่ฝ้า
2. กลับไปที่เครื่องมือ **เลือก** แล้วคลิกฝ้าที่ต้องการ
3. กดปุ่ม **แก้ขนาด/ระดับ**
4. เลื่อน Slider หรือกด **ขึ้น 10 ซม. / ลง 10 ซม.**
5. สามารถกรอกความสูงเป็นเมตร หรือใช้ค่า 2.4, 2.8 และ 3.2 เมตรได้

คีย์ลัด `Page Up` และ `Page Down` ใช้เลื่อนฝ้าที่เลือกครั้งละ 10 เซนติเมตร

## การเริ่มคำนวณราคา

- โมเดล 3D ที่สร้างจากผลสแกนจะแสดงวัสดุเริ่มต้นเพื่อให้ดูรูปทรงได้ แต่ **ยังไม่คิดราคา**
- ระบบจะเริ่มเพิ่มรายการลง Budget/BOQ เมื่อผู้ใช้เลือกวัสดุและกดใช้กับวัตถุที่เลือก หรือใช้กับทั้งหมด
- พื้นและฝ้าที่สร้างใหม่ก็จะยังไม่คิดราคา จนกว่าจะเลือกและใช้วัสดุจริง
- ยอดรวมและไฟล์ CSV จะรวมเฉพาะวัตถุที่ผู้ใช้ยืนยันวัสดุแล้วเท่านั้น
